<form method="post" action="{{ path('credit_card_delete', {'id': credit_card.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ credit_card.id) }}">
    <button class="btn">Delete</button>
</form>
