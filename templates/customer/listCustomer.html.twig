{% extends 'base.html.twig'%}

{% block title %} Liste Clients {% endblock %}

{% block body %}
    <!-- Breadcrumb Section Begin -->
    <div class="breacrumb-section">
        <div class="container">
            <div class="row">
                <div class="col-lg-12">
                    <div class="breadcrumb-text product-more">
                        <a href="{{ path('main') }}"><i class="fa fa-home"></i> Home</a>
                        <a href="{{ path('list_customer') }}">Customer</a>
                        <span></span>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- Breadcrumb Section Begin -->

    <section class="checkout-section spad">
        <div class="container">
            <div class="row">
                <div class="col-lg-12">
                    <h4>Liste des Clients</h4>
                    <div class="table-responsive">
                        <div class="toolbar">
                            <p data-placement="top" data-toggle="tooltip" title="Ajouter"><button class="btn btn-success btn-xs" data-title="Ajouter" data-toggle="modal" data-target="#ajouter" ><span class="fa fa-plus-circle"></span>Ajouter Client</button></p></td>
                        </div>
                        <table id="mytable" class="display">
                            <thead>
                                <tr>
                                    <th>id</th>
                                    <th>Client</th>
                                    <th>email</th>
                                    <th>Modifier</th>
                                    <th>Supprimer</th>
                                </tr>
                            </thead>
                            <tbody>
                            <tr>
                                <td>1</td>
                                <td>Nom Prénom</td>
                                <td>email</td>
                                <td><p data-placement="top" data-toggle="tooltip" title="Edit"><button class="btn btn-primary btn-xs" data-title="Edit" data-toggle="modal" data-target="#edit" ><span class="fa fa-plus-circle"></span></button></p></td>
                                <td><p data-placement="top" data-toggle="tooltip" title="Delete"><button class="btn btn-danger btn-xs" data-title="Delete" data-toggle="modal" data-target="#delete" ><span class="fa fa-times-circle"></span></button></p></td>
                            </tr>
                            <tr>
                                <td>2</td>
                                <td>Nom Prénom</td>
                                <td>email</td>
                                <td><p data-placement="top" data-toggle="tooltip" title="Edit"><button class="btn btn-primary btn-xs" data-title="Edit" data-toggle="modal" data-target="#edit" ><span class="fa fa-plus-circle"></span></button></p></td>
                                <td><p data-placement="top" data-toggle="tooltip" title="Delete"><button class="btn btn-danger btn-xs" data-title="Delete" data-toggle="modal" data-target="#delete" ><span class="fa fa-times-circle"></span></button></p></td>
                            </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        $(document).ready(function () {
            $('#mytable').DataTable();
        });

        $('#mytable').dataTable( {
            "searching": false,
            "lengthChange": false
        } );

        function editFormatter(){
            var route = '';
            return route.replace()
        }
    </script>
{% endblock %}