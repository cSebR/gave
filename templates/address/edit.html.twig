{% extends 'base.html.twig' %}

{% block title %}Edit Address{% endblock %}

{% block body %}
    <div class="col-lg-6">
        <h2>Edit Adresse</h2>
    </div>
    <div class="col-lg-4">
        {{ include('address/_form.html.twig', {'button_label': 'Update'}) }}
    </div>
    <div class="row">
        <a href="{{ path('address_index') }}" class="site-btn place-btn">back to list</a>
        {{ include('address/_delete_form.html.twig') }}
    </div>


{% endblock %}
