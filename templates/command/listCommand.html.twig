{% extends 'base.html.twig'%}

{% block title %} Liste Commande {% endblock %}

{% block body %}
    <!-- Breadcrumb Section Begin -->
    <div class="breacrumb-section">
        <div class="container">
            <div class="row">
                <div class="col-lg-12">
                    <div class="breadcrumb-text product-more">
                        <a href="{{ path('main') }}"><i class="fa fa-home"></i> Home</a>
                        <a href="{{ path('list_command') }}">Command</a>
                        <span></span>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <!-- Breadcrumb Section Begin -->

    <section class="checkout-section spad">
        <div class="container">
            <div class="row">
                <div class="col-lg-12">
                    <h4>Liste des Commandes</h4>
                    <div class="table-responsive">
                        <div class="toolbar">
                            <p data-placement="top" data-toggle="tooltip" title="Ajouter"><button class="btn btn-success btn-xs" data-title="Ajouter" data-toggle="modal" data-target="#ajouter" ><span class="fa fa-plus-circle"></span>Ajouter Commande</button></p></td>
                        </div>
                        <table id="mytable" class="display">
                            <thead>
                                <tr>
                                    <th>N°Commande</th>
                                    <th>Client</th>
                                    <th>Status</th>
                                    <th>Date</th>
                                    <th>Annuler</th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr>
                                    <td>1</td>
                                    <td>Nom Prénom</td>
                                    <td>EN Cours</td>
                                    <td>07/01/2019</td>
                                    <td><p data-placement="top" data-toggle="tooltip" title="Delete"><button class="btn btn-danger btn-xs" data-title="Delete" data-toggle="modal" data-target="#delete" ><span class="fa fa-times-circle"></span></button></p></td>
                                </tr>
                                <tr>
                                    <td>2</td>
                                    <td>Nom Prénom</td>
                                    <td>EN Cours</td>
                                    <td>07/01/2019</td>
                                    <td><p data-placement="top" data-toggle="tooltip" title="Delete"><button class="btn btn-danger btn-xs" data-title="Delete" data-toggle="modal" data-target="#delete" ><span class="fa fa-times-circle"></span></button></p></td>
                                </tr>
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        $(document).ready(function () {
            $('#mytable').DataTable();
        });

        $('#mytable').dataTable( {
            "searching": false,
            "lengthChange": false
        } );
    </script>
{% endblock %}